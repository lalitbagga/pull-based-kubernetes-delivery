apiVersion: v1
kind: Service
metadata:
  name: {{ include "delivery-api.fullname" . }}
  labels:
    {{- include "delivery-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "delivery-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
